import 'package:flutter/material.dart';

// 🌌 BRAINOVA - Thème spatial futuriste

// Couleurs principales
const kPrimaryColor = Color(0xFF1A1F3A);        // Bleu nuit profond (fond principal)
const kSecondaryColor = Color(0xFF2D3561);      // Bleu spatial moyen
const kAccentColor = Color(0xFFFFD700);         // Doré lumineux (étoile/nova)
const kAccentPurple = Color(0xFF8B5CF6);        // Violet spatial
const kAccentPink = Color(0xFFEC4899);          // Rose néon

// Couleurs de fond
const kBackgroundColor = Color(0xFF0F1419);     // Noir spatial
const kSurfaceColor = Color(0xFF1E2338);        // Surface des cartes
const kWhiteColor = Color(0xFFFFFFFF);          // Blanc pur

// Couleurs de texte
const kTextPrimary = Color(0xFFFFFFFF);         // Texte principal blanc
const kTextSecondary = Color(0xFFB0B8D4);       // Texte secondaire gris-bleu
const kTextMuted = Color(0xFF6B7280);           // Texte atténué

// Boutons
const kMainButtonColor = Color(0xFFFFD700);     // Bouton principal doré
const kSecondaryButtonColor = Color(0xFF8B5CF6); // Bouton secondaire violet
const kButtonTextLight = Color(0xFF1A1F3A);     // Texte sur bouton clair
const kButtonTextDark = Color(0xFFFFFFFF);      // Texte sur bouton foncé

// Carousel/Navigation
const kCarouselActiveLine = Color(0xFFFFD700);  // Ligne active dorée
const kCarouselInactiveLine = Color(0xFF2D3561); // Ligne inactive bleue

// États et feedbacks
const kSuccessColor = Color(0xFF10B981);        // Vert succès
const kWarningColor = Color(0xFFF59E0B);        // Orange warning
const kErrorColor = Color(0xFFEF4444);          // Rouge erreur
const kInfoColor = Color(0xFF3B82F6);           // Bleu info

// Effets lumineux (pour animations)
const kGlowYellow = Color(0xFFFFD700);          // Lueur dorée
const kGlowPurple = Color(0xFF8B5CF6);          // Lueur violette
const kGlowPink = Color(0xFFEC4899);            // Lueur rose

// Dégradés (pour backgrounds)
const kGradientStart = Color(0xFF1A1F3A);       // Début gradient
const kGradientMiddle = Color(0xFF2D3561);      // Milieu gradient
const kGradientEnd = Color(0xFF1E2338);         // Fin gradient

// Shadows
const kShadowColor = Color(0x40000000);         // Ombre douce
const kGlowShadow = Color(0x80FFD700);          // Ombre lumineuse dorée